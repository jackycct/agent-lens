{
  "name": "@avionics/flight-recorder",
  "version": "0.1.0",
  "description": "Avionics Flight Recorder CLI for agentic engineering evidence and benchmarks.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "flight-recorder": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/unit/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "ajv": "^8.17.1",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
